logging.level.com.sap.cloud.sdk.cloudplatform.connectivity=DEBUG
logging.level.com.sap.cloud.sdk.datamodel.odata=DEBUG

# Disable Spring's auto-configured multipart settings
spring.servlet.multipart.enabled=false

# Manual configuration (redundant but ensures both approaches work)
multipart.maxFileSize=100MB
multipart.maxRequestSize=100MB